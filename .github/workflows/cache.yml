name: Cache-Stuff
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

on:
  push:
  workflow_dispatch:
  workflow_run:
    workflows: [update-flake-lock]
    types:
      - completed

permissions:
  contents: read

env:
  NIXOS_HOSTNAME: "Ainz-NIX"
  NIXOS_USER: "masum"
  NIXOS_ATTRS_LIST: >
    programs.hyprland.package
    programs.hyprland.portalPackage
    services.ananicy.package
    programs.appimage.package
    boot.kernelPackages.kernel
  NIXOS_PKGS_LIST: >
    zed-editor_git
    scx.full
  CACHIX_AUTH_TOKEN: "${{ secrets.CACHIX_AUTH_TOKEN }}"
  CACHIX_CACHE_NAME: "johnrtitor"
  REQUIRED_PACKAGES: >
    cachix
    jq
    findutils

jobs:
  cache:
    name: Cache Stuff
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          sparse-checkout: |
            .github/actions

      - name: setup nix
        uses: ./.github/actions/setup-nix

      - uses: actions/checkout@v4

      - name: Install required packages
        run: |
          for PKG in $REQUIRED_PACKAGES; do
            nix profile install nixpkgs#$PKG
          done

      - name: Build Packages and push to Cachix
        id: build
        run: |
          chmod +x ./.github/scripts/build-and-cache.sh
          ./.github/scripts/build-and-cache.sh
      - name: Upload build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: build-logs-${{ github.run_number }}
          path: |
            build.log
            errors.log
            success.txt
            failures.txt
          retention-days: 30
          if-no-files-found: ignore
      - name: Upload build summary
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: build-summary-${{ github.run_number }}
          path: |
            success.txt
            failures.txt
          retention-days: 7
          if-no-files-found: ignore
